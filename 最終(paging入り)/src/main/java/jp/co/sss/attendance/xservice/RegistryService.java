package jp.co.sss.attendance.xservice;

import org.springframework.stereotype.Service;

@Service
public class RegistryService {
//	public Page<TaskInfoRegistry> findPaginated(Pageable pageable) {
//		@Autowired
//	    private RegistryRepository repo;
//		@Override
//	    public Page<TaskInfoRegistry> getPaginatedRegistries(Pageable pageable) {
//	        return repo.findAll(pageable);
//	    }
//	}
//	}
//		int pageSize = pageable.getPageSize();
//		int currentPage = pageable.getPageNumber();
//		int startItem = currentPage * pageSize;
//		List<Book> list;
////		        if (books.size() < startItem) {
////		            list = Collections.emptyList();
////		        } else {
////		            int toIndex = Math.min(startItem + pageSize, books.size());
////		            list = books.subList(startItem, toIndex);
////		        }
//		Page<TaskInfoRegistry> registryPage
//			= new PageImpl<TaskInfoRegistry>(list, PageRequest.of(currentPage, pageSize), books.size());
//		
//		return registryPage;
//		    }
}
